export const config = {
	API_URL: 'http://localhost:5000',
	MONGO_CONNECTION:// não foi possível incluir, pois o sistema do mongo não carregavaa tela para criação do cluster
		'mongodb+srv://<user>:<password>@<database>.yptlq.mongodb.net/url-shortener-dio?retryWrites=true&w=majority',
}
